# Personal Site

Static personal homepage for Juan Zaragoza. Served via GitHub Pages from this repository.

## Local preview
Open `index.html` in a browser or run a simple server:
```bash
python -m http.server 8000
```
Then visit http://localhost:8000/.

## Deploy
Commit and push to the `main` branch; GitHub Pages will serve the latest `index.html`.
